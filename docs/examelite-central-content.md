# Central exam controls and question transfers

This is the first part of the revised integration. It is implemented locally and requires explicit user deployment. It does **not** complete the full in-page ExamElite authoring and exam-taking requirement.

## Confirmed product direction

Exam tools belong inside Tech4Learn's organisation navigation and domain. ExamElite remains the engine and data authority for questions, exams, attempts and marking. Neither external workspace launch links nor an iframe constitute the desired final interface. Reuse its terminology and workflow alongside the existing shared Tech4Learn form/table template.

Superadmin controls platform availability, organisation/plan entitlements, staff permissions and central content distribution. Platform availability and commercial plan configuration are separate from an organisation's enabled modules; a provider capability must not be labelled working before its adapter and interface are complete.

Central questions can be shared into selected organisations. Superadmin can also pull organisation questions into the central bank. Each operation creates an independently owned copy with provenance; it never transfers ownership or overwrites an original. Only superadmin changes central content. Student identity and programme records remain in Tech4Learn; no reverse import of ExamElite students is implied.

## Working in this change

- Superadmin can create and edit central exams in the shared internal builder, assemble owned central questions, manage sections and timers, and change publication/result visibility. The gateway validates native snapshots, and central drafts and routes stay separate from organisation edits. Central PDF and translation controls remain unfinished.
- Superadmin can enable the organisation's Exams module using the existing versioned module settings. The toggle preserves other modules and branding. Exam permissions now enforce this module flag at the API boundary.
- The feature-control form shows saved access beside implemented tools and remaining gaps for each of the five groups. Bulk allow/restrict selections remain local until Save. Reload retrieves the current revision after a conflict; recovered drafts from an older revision are rejected. Checkbox values use the scoped controlled draft rather than independent DOM restoration. A synthetic browser check covers stale recovery, explicit bulk saving and reload. This is a feature coverage view, not the unfinished commercial plan catalogue or plan assignment system.
- Superadmin can search the central or selected organisation question bank, select up to 50 questions and share or pull them. Organisation users can view only their own question bank, with module, permission, organisation scope and question-feature checks.
- Questions are paged by stable ID, 50 at a time; search executes in ExamElite. Table filters apply to the explicitly labelled loaded collection. List snippets are escaped text, not executable provider HTML.
- Transfers validate the complete selection against the source tenant before copying. The existing additive ExamElite copy service preserves question configuration, translations and taxonomy, including numeric answer settings. Central pulls reuse existing central language identities without overwriting them.
- A per-workspace request ID makes a lost-response retry safe. A separate copy map preserves subsequent destination edits even when a new transfer request selects the same source again. Reusing a request ID with changed input is rejected.
- The ExamElite transfer table records direction, actor UUID, source/copy IDs and time. The superadmin screen exposes the latest 50 transfers. Tech4Learn also writes an audit event; remote success followed by a local audit failure can be retried safely.
- Sharing can provision an isolated exam organisation through the existing private identity bridge without issuing a launch ticket. Credentials and native session details never reach these screens.
- The organisation UI no longer displays external authoring/student launch buttons. Existing linked result reads remain available.
- Staff with exam management access can create and edit an organisation-owned question inside Tech4Learn: basic formatted text, options, answers, hints, explanations and marks. Changes invoke the installed ExamElite controller with a private mapped staff context. Native validation remains authoritative.
- Authoring uses a revision check, an atomic native transaction and a durable request ledger for safe retries. Unsupported fields cannot change ownership. The API derives the actor from the authenticated session and enforces module, organisation and question restrictions.
- Native authoring retries recheck the active organisation, active native user and mapped staff membership before returning a stored outcome. Disabling the native account or revoking its staff mapping therefore also blocks a retry of a previously successful save. Isolated tests verify denied retries leave content and the request ledger unchanged, and that restoring access preserves the original outcome. Translation review, approved-document reads/status and result marking also require the native staff account to remain active, independently of active organisation membership; native tests cover denial and marking replay after account suspension.
- Question types, language and classification use bounded, searchable native choices scoped to the organisation. Creation captures the model from the native event dispatcher rather than guessing a latest ID. Its request ledger prevents duplicate creation on retries.
- Classification create/update reuses native controllers and the same revision/transaction/retry controls. The Subjects feature restriction is enforced separately from the Questions feature restriction. Existing subject category assignments are preserved.
- The exam builder creates and updates organisation exams using the native ExamController: type/syllabus, groups/packages/languages, schedules, timing, passing percentage and delivery flags. It lists owned exams and adds/removes selected owned questions through the native paper actions. The adapter checks the entire question selection before writing and keeps durable retry IDs. Removing a question preserves the bank record.
- Standalone exams can select an owned category and subcategory. The subcategory lookup is filtered by an authorised parent category, and changing the parent clears the prior child selection. Lookup responses from an earlier selection are ignored. Package-based exams keep native package-derived classification. Native validation rejects foreign or mismatched categories without changing the exam; local browser checks cover a delayed lookup response and the saved category fields.
- Organisations can enable an available central language and edit its local True/False labels through the native LanguageController. The enable list excludes already enabled language codes and other organisations. Re-enabling a previously disabled language preserves its native ID and resets labels from the central definition, as the native controller does. Names and codes cannot be changed through this organisation adapter, and a native platform-admin context is rejected. Durable retries do not duplicate the language. Central language administration remains unfinished. Native tests use isolated identity/configuration helpers; browser checks verify the enable and label-only payloads.
- Language disabling uses the same native organisation controller with a revision and durable request ID. It retains the language record and existing content; a repeated disable does not re-enable or delete it. The API verifies the returned language ID, revision and disabled flag before reporting success. The form locks editing after an uncertain response and permits an identical retry or reload. Native checks cover record retention, central-record denial and repeat operations; a synthetic browser check covers a lost response and retry.
- Free package details can be created and edited through the native PackageController: name, description, groups, category/subcategory, existing or new organisation tags, expiry and active status. Existing linked-exam order, translations, photo references and document settings are preserved. Exam links remain managed from the exam editor, so package-detail saves do not resubmit its exam pivot. Foreign or unavailable numeric tags are rejected rather than silently removed. Native checks cover preservation, replay, foreign selections and paid-package rejection; a synthetic browser checks create selections and patch-only updates. Paid-package management and package-specific document workflows remain unfinished; bounded package image preview and changes are described below.
- The editor uses the shared DraftForm and DOMPurify to sanitise native HTML. DOMPurify is added for this concrete rich-text rendering use case. TeX formula text can be edited and previewed in place with the same bounded renderer used by students. A formula helper offers fraction, power, root, sum, integral, matrix and chemistry examples, checks the edited TeX, previews it and explicitly appends its source to the field. Changing the formula or display mode invalidates the previous check. Temporary composer inputs stay out of recovered drafts; insertion updates the normal field draft. Preview never replaces editable source with rendered markup. Native tests cover formula/entity preservation, answer preservation and save retries; synthetic browser checks cover the example set, invalid commands, draft exclusion, insertion and source preservation. Existing image/MathML/math-field markup remains read-only and preserved by the text editor; image uploads use the separate scoped form described below. Visual editing of existing native formula markup remains unfinished.

Existing question, option, model-answer, hint and explanation images have a separate hashed preview. The browser loads their raster bytes through a same-domain endpoint that checks organisation-wide exam permissions, module state and Questions restrictions before and after the provider call. The provider verifies the active workspace owner and that the image is still referenced by that question. Responses are bounded, uncached and audited by identifiers. Original source fields remain in the editable snapshot for patch-only preservation, but the preview never loads those storage URLs directly. Native checks cover foreign/inactive owners, restrictions, removed/unreferenced images and invalid raster data; HTTP checks cover scope, response validation and mid-request revocation. A synthetic browser check covers image rendering without enabling edits. Upload and replacement use the separate form described below.

## Image changes and remaining release work

The private package-image write adapter now handles a first upload, explicit replacement of the current image hash, and reference-only removal. It shares the bounded PNG/JPEG/WebP validation used for question images (512 KB, 20 million pixels), creates a random native package-upload filename and updates only the native Package model's photo attribute inside the existing scoped authoring transaction/request ledger. The native web controller deletes previous files, so this adapter deliberately retains them for other copied packages. Failed transactions remove only the newly created file. Native tests cover current-photo checks, source preservation, preview compatibility, duplicate retries, removal, forbidden native context, revoked staff and cleanup. The authenticated Tech4Learn upload API and saved free-package form now connect this service. The API accepts only bounded image/hash/removal fields, derives the actor from the staff session, rechecks capability access after the provider call and verifies the saved package identity, revision and resulting image reference. The shared image form preselects replacement for an existing package image, supports reference-only removal, requires other package changes to be saved first and locks ordinary editing while an upload outcome is uncertain. It retains identical retry payloads; confirmed reload clears the pending state even when the record revision is unchanged. File bytes remain in memory and never enter recovered drafts. HTTP tests cover invalid fields, actor/path overrides, provider mismatch, revocation and capability denial; synthetic browser checks cover replacement, removal, identical retries, editor locking and draft exclusion, plus the existing question-image flow. Abrupt process termination can still leave an orphan file, and retained-file reconciliation remains unfinished.

A private native package-photo reader is implemented, with an opaque current-photo hash in package snapshots. It resolves only the owned package's stored photo under the native public package-upload directory, bounds raster size, rejects paths/URLs supplied as query overrides, and rechecks workspace restrictions, active ownership and the current photo after reading. The new route uses the dedicated central credential and existing media throttle. Local tests cover real synthetic raster bytes, foreign ownership, disabled organisations, restrictions, invalid images, traversal, missing files and a photo changed during reading. The authenticated Tech4Learn API now serves those bytes through the organisation domain, requires exam management and the Subjects capability used by package authoring, validates the provider package/hash/raster envelope, audits identifiers and rechecks access after the provider responds. Saved package forms display the current image with explicit retry after a load failure. HTTP tests cover scope, query overrides, invalid provider replies, restrictions and revocation; a synthetic browser checks the same-domain image and retry. Package-image upload, replacement and reference-only removal are connected through the separate form described above.

Package forms can now add named organisation tags alongside existing selections. Adding a tag stages it locally; the native PackageController creates or reuses the organisation's tag only when the package is saved. Pending names and selections use the scoped revision-aware draft. Untouched selections are retained, and newly staged tags can be removed before saving. Numeric identifiers still require an available owned or central tag; new names cannot revive disabled tags or modify a foreign tag. Native tests cover ownership, replay, duplicate names, rejected markup/empty slugs and unchanged selections after rejection. A synthetic browser check verifies explicit selection before save, preservation of existing tags and patch-only subsequent edits.

The private `save-exam-translation` action and same-domain editor now support translated exam titles, instructions and syllabus. The inspected native engine has no manual exam-language controller, so its add-on saves through the native `ExamLanguageTranslation` model and uses the native fingerprint and document-invalidation services. Only submitted fields change; owned assigned non-English language, active staff, current paper/review revisions, package ownership and no active translation job are required. Partial edits certify explicitly edited source fields and fields empty in both versions; legacy translations without per-field evidence may require renewed review. Saves clear that language's approval, invalidate native PDFs and replay safely through the existing request ledger. The shared editor retains actor/organisation/record-scoped drafts, rejects drafts from an older review, preserves untouched images and retries an uncertain save with the same request. Native tests cover preservation, partial completeness, invalid-title rollback, stale revisions and replay. Real worker/PDF verification and rich image/formula translation authoring remain outstanding.

The private `save-question-translation` action now delegates basic translated question wording to the native QuestionLangController. It patches only submitted fields, preserves untouched images and short-answer data, and records native per-field source fingerprints only for reviewed/current fields. It requires the current paper review, assigned non-English language, owned attached question and active staff context; processing translations and cross-organisation question references are rejected. Changed translations clear affected owned-paper approvals and invoke native PDF invalidation. Synthetic tests cover native create/update, unchanged source questions, untouched-field preservation, partial translation completeness, validation rollback and request replay. The authenticated editing API now validates exam/language/question IDs, the reviewed translation revision and a bounded patch of supported wording fields; the staff actor comes from the session. Unsupported fields and malformed patches are rejected before the provider call. The selected non-English question now has a basic formatted-text editor inside the review. It submits only changed fields, locks navigation while editing, preserves identical uncertain-save retries, and uses actor/organisation/record-scoped drafts checked against both exam and translation revisions. Image and visual-formula fields remain read-only. A synthetic browser verifies patch-only writes, preservation, draft restoration, stale-draft rejection and identical retries. Rich image/visual-formula translation editing remains unfinished.

The private `refresh-translation` action now queues the installed ExamDocumentBulkActionService for exactly one owned exam/language, using its background translation mode. It requires the current review revision, incomplete wording, the organisation's native AI-translation plan feature and the mapped staff context. Native package ownership checks also apply. The existing request ledger prevents a lost-response retry from dispatching twice. Synthetic tests exercise the real native bulk service with counted job dispatch, including plan denial and stale-review rejection; no AI provider or worker is run. The authenticated API validates the selected language and reviewed revision. The review screen now requests refresh for missing/outdated translations, retains an identical uncertain request for retry, and blocks another request while the native state is processing. Explicit refresh resets the native failed-row stop state and prior approval before dispatch; the native worker otherwise ignores failed rows. Native automation settings still determine automatic approval/PDF behaviour. A synthetic browser verifies refresh routing, lost-response retry and processing state. Successful queueing does not establish successful translation or PDF generation.

The private `approve-translation` exam action invokes native ExamDocumentController approval only after the exact reviewed translation revision is still current and the native completeness check passes. Incomplete translations are rejected rather than entering the native automatic refresh-and-approve branch. Owned package validation protects automatic PDF dispatch; native auto-PDF settings remain authoritative. The authoring ledger replays a lost response without repeating PDF requests, and active staff access is checked before replay. Native tests use the installed approval controller and fingerprint logic with counted job/lifecycle doubles, covering approval identity, stale/incomplete rejection, foreign-package denial and revoked retries. The authenticated Tech4Learn action validates the selected language and review revision and derives the actor from the session. The review screen exposes approval only for current, complete, unapproved wording, with an explicit review confirmation that is never stored in a draft. An uncertain response locks the selection and retains the identical request for retry; explicit reload checks the saved state. Synthetic browser checks cover confirmation, identical retry, already-approved state and incomplete-translation gating.

The private translation-review adapter reads an owned paper and assigned enabled language using ExamElite's native translation completeness and per-field fingerprints. It returns source/target wording, stale fields, native approval state and bounded pages of 50 questions. A paper-wide revision covers source, target and approval changes; later pages require the same revision and reject changes instead of mixing review versions. Dedicated credentials, mapped active staff, organisation ownership and the Exams restriction are checked before reading. Synthetic native tests cover missing/complete/stale translations, English source semantics, revision changes, pagination and access denial. The authenticated Tech4Learn API derives the actor from the session, checks exam access before and after the provider call, validates IDs, revisions, counts, wording and cursor order, and projects only review fields. No AI translation or approval is triggered by either reader. Review wording now rewrites image sources to opaque references. A private media reader serves only raster images referenced by that exact exam/question translation review, checks the paper revision and staff access again after reading bytes, and uses the existing confined media resolver. Native tests cover translated and exam-level images, unreferenced/foreign selections, removed references and revocation during the read. Each media read currently revalidates the bounded paper snapshot; performance on large papers remains to be measured. The authenticated same-domain media route now validates exam, language, question, revision, asset and raster envelope, audits IDs, and rechecks access after the native call. Saved exams expose a read-only translation review with assigned-language selection, source/target rich text, maths, scoped images and revision-bound pagination. Changing language or unsaved exam settings clears the prior review; a stale page response requires restarting the review. A synthetic browser check covers language scope, maths, private-image loading, cursor revisions and conflict reset. Basic exam-level translated wording now has a scoped editor; image and visual-formula fields remain read-only. Returned wording always uses the existing sanitised rich-text renderer, never unrestricted HTML or external storage URLs.

The private document-status endpoint projects the selected native build as not built, queued, processing, ready, stale or failed. Approved-file availability is reported separately, because a previous approved artifact can remain downloadable during a replacement or after its failure. Availability checks confine the file to native PDF storage and check its size and signature. Status reads enforce the same staff, organisation, feature and selection scope as downloads and omit native paths and raw worker errors. Native tests cover missing builds, replacements, malformed files, restricted reads and dedicated credentials. The authenticated Tech4Learn status endpoint now validates selection IDs, native state and availability, rechecks access after the provider call and returns only the projected state fields. HTTP tests cover scope, invalid provider states and mid-request revocation. Saved exams have an explicit Check PDF status action for both paper and solutions; changing selection or requesting generation clears the prior display. A synthetic browser check covers processing and failed replacements with retained approved availability. This is an on-demand snapshot, not automatic polling or proof that a queued job has finished.

The private `generate-document` exam action now invokes the installed native ExamDocumentController with the mapped organisation staff context. It requires an owned assigned package and an enabled assigned language; the native controller enforces translation approval and nonempty-paper rules before queueing. Existing exam revisions and the durable authoring request ledger protect retries, including revocation before replay. Isolated tests use the unmodified controller/lifecycle with fingerprint and job-dispatch doubles, covering one queued build, unchanged retry, foreign selection, missing approval, disabled language and empty paper. No real renderer or worker runs in these tests. The authenticated Tech4Learn exam-action API now forwards this action with the session-derived actor, bounded package/language IDs and exact document type. It enforces the Exams restriction and rejects malformed fields before contacting the provider. Saved-exam controls now request paper or solution generation for the selected assigned package/language. A failed response keeps the identical request in memory and locks selections until retry or explicit reload; confirmed validation/conflict errors allow correction. A synthetic browser check verifies the locked selections and identical lost-response retry. Actual generated-file verification remains unfinished.

Package forms also expose native question-paper and solution PDF visibility, title, header, footer and watermark settings. They use the same scoped revision-aware draft and patch-only save; native length validation and retry handling remain authoritative. Native tests cover every document setting, replay and rejection without partial writes; a synthetic browser check covers visibility and text payloads. Generation and approved downloads use the separate controls described below.

The private approved-document adapter now reads an artifact selected by ExamElite's native document lifecycle service. It checks the source platform, active organisation, mapped staff, Exams restriction and explicit paper/package/language relationships. It preserves the native last-approved-artifact behaviour during replacement processing. Files must resolve within native PDF storage, have a PDF signature and fit the 10 MB bound; responses contain bytes and identifiers, never storage paths or external redirects. Dedicated-credential tests use the unmodified lifecycle service with synthetic records/files and cover ownership, restrictions, language availability, invalid files and missing approvals.

The authenticated Tech4Learn endpoint now serves these approved documents as same-domain PDF attachments. It derives the staff actor from the session, requires organisation-wide exam management access, enforces module and Exams restrictions before and after the provider call, limits requests and audits identifiers. It verifies the returned exam, package, language, document type, build ID, MIME, canonical base64 and PDF signature before releasing bytes. Query fields cannot override the actor or supply a path. HTTP tests cover attachment headers, foreign-organisation denial, malformed selections, invalid provider replies and membership revocation during the call. Saved exams now expose approved-paper and approved-solution downloads. Searchable selectors offer only assigned packages and languages; server validation remains authoritative. Downloads require saved settings, reject redirects and non-PDF responses, and use temporary local blob links with safe filenames. Leaving the component aborts the request. A synthetic browser check covers scoped selections, download filenames and missing-approval feedback. None selects an artifact without that package/language variant. Real generated-file verification remains unfinished.

The image form also removes a selected image reference without uploading a file. Removal uses the same native revision and retry ledger, requires a hash present exactly once in the chosen field, and rejects requests mixing removal with new image bytes. Stored files are retained for other references. Native and browser tests cover reference-only removal and identical retry; HTTP tests reject missing or ambiguous operation fields.

The image-write backend and its browser form are implemented locally. It accepts one PNG/JPEG/WebP up to 512 KB and 20 million pixels for a saved owned question, with a field name and optional existing image hash to replace. It reuses ExamElite's public storage disk and native question-update controller. Workspace, active staff, revision and request-ledger checks run before writing. Repeating the request returns its prior result without storing again; a native-save exception rolls back the question and removes the new file. Replacement retains the old file because another copied question may still reference it. An abrupt process/host termination between storage and transaction completion can leave an unreferenced file; reconciliation is not implemented yet. The HTTP route validates bounded fields, derives the actor, rechecks access after the native response and audits identifiers only. Isolated native tests use a storage double and verify append, replacement, retries, invalid inputs and failure cleanup. The shared draft form offers append or replacement within a chosen question field, with a local file preview. Only destination choices enter the scoped draft; file bytes and pending request payloads stay in memory. Other question changes must be saved first. An uncertain upload freezes ordinary editing and retains the identical retry request; reload reads the saved version before another attempt. Successful writes refresh the question and private preview. Synthetic browser checks cover file-size rejection, editor locking, identical retry, draft exclusion and replacement targeting. Real deployment and representative media checks remain outstanding.

Superadmin can preview a central question's full wording, supported answer values and protected images before sharing it. The dedicated native read route derives its owner only from the central credential, rejects organisation/path overrides and requires an active central organisation. Image requests include the preview revision and recheck credential, ownership and revision after reading. The Tech4Learn API requires current superadmin status before and after the provider call, validates returned identifiers and raster envelopes, limits requests and audits identifiers. The internal preview reuses the question renderer, keeps images on the version-specific same-domain API and clears an old preview when a reload fails. Native tests cover owner isolation and stale media; HTTP tests cover non-superadmin denial, revoked authority and malformed replies; a synthetic browser check covers sanitisation, private-image URLs and reload recovery. This preview does not yet edit shared originals.

A private native adapter can now create and edit central questions through ExamElite's existing question controller. Its central owner comes only from the dedicated server credential. It creates a private native author mapping for the authenticated Tech4Learn actor supplied by the gateway, with central-organisation membership and no global administrator flag. Native account/membership suspension blocks writes and successful-request replays. Separate central mapping and request tables avoid treating the central bank as an organisation workspace. Provisioning, native validation, question writes and the retry ledger share one transaction; failed validation rolls back a newly created author. Native checks cover ownership, unchanged organisation copies, stale edits, field overrides, provisioning rollback, retries and request-context restoration. This additive schema requires user deployment. The Tech4Learn superadmin create/update API now connects this adapter. It derives the actor from the session, checks current superadmin authority before and after the native request, rejects actor/owner overrides and unsupported or oversized fields, preserves request identity, and verifies the saved question ID, revision and snapshot. Native validation and stale-edit feedback remain distinguishable from an uncertain provider response. HTTP tests cover creation, updates, identical retry payloads, authority revocation, override rejection and malformed replies. A private central classification endpoint now reuses the native owner-scoped choice query for groups, subjects, sections, topics, subtopics, enabled languages, types and difficulties. Its owner comes solely from the dedicated credential; it rejects owner overrides, bounds search and cursor pages, and requires an active central organisation without provisioning a tenant workspace. Native tests cover isolation, pagination, malformed queries and inactive-owner denial. The authenticated gateway now exposes those central selectors only to current superadmins, rechecks authority after native reads, rejects owner/query overrides and validates bounded, ordered choice pages and cursors. It returns only supported selector fields and audits the classification kind without recording search text. HTTP tests cover access, revocation, forwarding, response projection and malformed or repeated pages. Superadmin can now choose Edit original to open the shared question editor inside Tech4Learn. Central classification requests use the dedicated central gateway; drafts have a separate central namespace and are revision checked. Patch-only saves preserve untouched answers and media, failed saves retain drafts and request identity, and images use version-specific central API URLs. A failed reload removes the stale central form. Synthetic browser checks cover these behaviours. Superadmin can also create a new central question using the same classification, wording, formula, answer and marking controls. Opening the empty draft creates no native account or tenant workspace; only the checked create action writes through the native adapter. The returned native question ID opens its saved editor. HTTP checks cover draft access and override rejection, and a synthetic browser check creates a numerical question with central classifications and verifies its saved answer. The private native central adapter now supports bounded question-image upload, replacement and removal through the existing image helper and native question controller. It verifies the active central author, owned question and revision before writing, includes the action in request fingerprints, rolls back failed image writes, and retains old files that organisation copies may still reference. Native tests cover scope, unchanged answers/copies, duplicate requests, stale revisions, revoked authors, failed native writes and removal. The authenticated gateway and shared image form now connect central upload, replacement and removal. The gateway derives the actor from the session, bounds field/image inputs, rejects overrides, rechecks superadmin authority after the native call and validates the saved snapshot. The image form uses a separate central draft namespace, keeps bytes and pending request state in memory, locks other editing during uncertain saves and refreshes revision-scoped image previews. HTTP tests cover image access, malformed inputs/replies and authority revocation. Synthetic browser verification covers uploads, identical retries, editor locking, draft byte exclusion, refreshed previews and removal.

The native formatted-text adapter now accepts bounded presentation MathML through the existing question controller. It permits the same presentation elements used by the student renderer, requires child formula elements to remain inside a math root, limits element count and attribute lengths, and rejects event handlers, links, arbitrary namespaces, CSS and annotation markup. Native checks verify a fraction and exponent survive saving without answer changes, identical retries, rejection rollback and existing translation workflows. The shared editor now lists existing top-level MathML formulas and previews the selected one. Staff can replace it with checked TeX using the same formula templates, preserving other formulas and surrounding text. Replacement becomes part of the ordinary revision-scoped question draft; selector and temporary formula input are excluded. Once no MathML remains, ordinary wording editing is available again. The adapter refuses to offer replacement for fields containing unsupported markup or images, avoiding a lossy conversion. Synthetic browser checks verify selected replacement and ordinary editor recovery; native tests verify mixed TeX/MathML saves preserve answers. Translated wording uses the same checked formula replacement. Its draft validator now accepts supported original MathML while still rejecting stale review revisions and unsupported image/formula fields. Native translation tests verify retained MathML and new TeX survive saving and replay; browser tests cover restoring a formula draft, unchanged image fields, uncertain-save retries and stale-draft rejection. Editing MathML internals directly, math-field markup and mixed image/formula fields remain unfinished.

The private central authoring service now also creates and updates central exam groups, subjects, topics, subtopics and question sections through their existing native controllers. It shares the restricted central-author mapping and transactional retry ledger, includes classification kind in request identity, validates ownership and native relationships, and preserves unchanged settings on edits. Native tests cover all five kinds, foreign relationships and records, stale edits, owner overrides, disabled-author retries and absence of tenant-workspace provisioning. Private central classification read/write routes now expose this service with kind-labelled snapshots and recoverable validation/conflict responses. Reads derive the owner from the credential, require an active central organisation and reject organisation/query overrides. Empty drafts do not provision native authors. Native route tests cover all five draft kinds, current revisions, foreign-record denial, inactive-owner denial and controller-backed writes. The authenticated Tech4Learn gateway now connects classification reads, new drafts, creation and updates. It derives the actor from the session, checks current superadmin authority before and after the native call, permits only the five supported kinds and their fields, bounds requests and verifies returned kind, record ID, revision and fields. Native conflict and validation feedback remain distinguishable from uncertain provider replies; audit records contain identifiers only. HTTP tests cover all five writes, draft reads, identical retries, malformed replies, scope overrides and mid-request authority revocation. The shared classification form is now connected in the superadmin interface for these five kinds. Lists, relationship choices and writes use the central gateway, and central drafts have a separate organisation-context namespace with revision checks. Unchanged fields are preserved on updates; uncertain saves retain request identity for retry. Failed reloads hide stale editing controls and offer a retry for the saved record. Synthetic browser verification covers central creation, draft scoping, identical retries, reload recovery and editing from the list. Central language administration remains outside this screen until its adapter is ready. Central categories and subcategories now use the same native controller, authenticated gateway and shared classification editor. Parent choices are scoped to an owned root category; foreign parents, child-as-parent selection and corrupt cross-owner parent links are rejected. Category edits retain group ordering and header/search metadata. Native subcategory enablement remains enforced, and failed writes leave no retry-ledger entry. Local native checks cover creation, edits, retries, stale records, ownership and preserved organisation records; HTTP checks cover parent-filter forwarding and rejection of malformed or overridden scope. A synthetic browser check covers central category creation, drafts, retry and reload recovery.

The native visual markup/formula authoring editor, the full plan/provider module catalogue, paid-package management, central language management, OMR/PDF workflows and representative end-to-end verification remain unfinished. The current question editor supports organisation-owned questions and superadmin edits to existing central originals. Classification screens support exam groups, categories and subcategories, subjects, topics, subtopics and question sections through native create/update actions. Category edits preserve native group ordering and header/search metadata; no group selection means organisation-wide availability. Subcategories use native parent validation and inherit group availability from their parent; the native subcategory feature setting is enforced during saves. Category deletion and central language administration are not yet editable here. Classification draft restoration uses controlled revision checks, without independently restoring stale DOM field values. Feature restrictions for the existing native backend are retained, but are not evidence that each replacement internal screen is ready.

Same-domain student sign-in, staff-issued exam links, start/resume, answer saving, submission, published result history and staff pending-answer marking now have local implementations and tests. ExamElite's native controllers still own attempts and grading. The student and marking renderer supports bounded formulas and private question/passage/reference images; this does not provide a rich authoring editor or student answer uploads. See [current student and marking status](examelite-student-attempts.md) for exact boundaries. The full integration is not ready for deployment as a completed release.

The external native workspace browser path is retired in the local implementation. Public launch requests return 410 without contacting the provider, and the provider refuses non-provisioning launch requests. Its early host middleware rejects application requests on old `t4l-*.examelite.com` hosts, including requests carrying previously valid sessions or tickets. Ordinary ExamElite hosts and private central API calls remain available. Private provisioning still creates isolated mappings but never issues tickets or grants all exam groups to a student. These changes take effect only after the user deploys both sides; the assistant has not changed live sessions. Existing ticket/session tables are retained, but they cannot restore the retired browser path. Same-domain access continues to enforce module, feature and grant checks.

## Deployment and verification history

The milestones below record the sequence of implementation. Earlier statements that a student screen or marking is unavailable describe that milestone, not the current status above. Test counts are also historical; the latest repository check passes 86 tests, with native-controller and synthetic browser checks run separately.

Development and tests run locally. Live access by the assistant is read-only. The user runs `deploy/examelite/deploy-central-content.sh` from a checked Git revision. The script requires the earlier native provider setup, preserves private credentials, installs additive sources, adds `tech4learn_content_transfers` and `tech4learn_authoring_requests`, verifies routes/schema and updates Tech4Learn. It does not install certificates or create DNS records.

`npm run check` covers the scaffold. `test-content-api.php` runs against an isolated SQLite database using ExamElite model definitions and tests complete-selection tenant checks, no partial copy on a foreign selection, request replay and pull provenance. `test-content-copies.php` additionally tests destination ownership, original preservation, native settings/translations and retry preservation. The local browser preview uses synthetic questions and checks enable/share/pull controls; it is not a live data or full exam workflow test.

`test-question-authoring.php` invokes the installed native question controller against synthetic SQLite records. It checks answer preservation across multiple-choice, true/false, blanks and numerical rules, native validation, foreign references, stale edits, replay, rejected ownership/markup changes and request-context restoration. Local checks also use a read-only snapshot of the current live controller/models. Host resolution and identity infrastructure are test doubles; this is not a live authoring test. A synthetic browser check covers patch-only saving, revision/request identity, validation draft retention and HTML sanitisation.

Creation/classification verification additionally covers native question translation creation, create retries, the original five taxonomy kinds plus categories and subcategories, subject edit scope preservation, foreign taxonomy rejection and restricted writes. The synthetic browser creates a numerical question and a subject through the internal forms. These are local checks, not proof of a live deployment.

The installer also fixes the verified native ExamController create-action bug where `passing_percentage` was read from an unassigned `$validated` variable. The transformation assigns the existing native validation result, is idempotent, rejects an unrecognised controller shape, and participates in the installer backup/rollback. This does not make the internal exam builder complete.

The builder verification runs the current native ExamController against isolated SQLite records, including exact fractional pass thresholds, owned group/language scope, repeat creates, settings edits, question add/remove and mixed-tenant selection rejection. Queue dispatch, paper cache invalidation and the native permission helper are isolated test doubles; Tech4Learn permission enforcement has separate HTTP tests. The synthetic browser creates an exam with a fractional threshold and adds/removes a question using refreshed revisions. Native delivery flags are stored engine settings, not evidence that the corresponding student workflow is available inside Tech4Learn yet.

Paper controls now include native section create/update/removal, assignment through organisation question-section definitions, per-subject durations, and desired Active/Inactive status. Complete subject selections are validated against the owned paper; negative, missing and foreign timers are rejected before native writes. Revisions include question-section pivots and saved timers. Status requests use desired state plus the request ledger, so repeat activation cannot toggle an exam off. These controls do not yet expose student attempts.

Native tests cover section duration overflow, foreign sections, assignment revision changes, publication retries, timer scope/overflow and empty-section removal. The synthetic browser verifies activation, section creation/editing, subject timer values, fresh revisions and successful draft clearing. The full scaffold check remains 82 passing tests.

The [student attempt adapter components](examelite-student-attempts.md) now have local native tests for answer persistence, submission/deadline rejection, retries and student-safe payloads. They are private components with no new public/student route; student authentication, explicit exam grants and the full attempt UI remain outstanding.

Migration 15 and the student entry now implement separate exam-scoped sign-in, expiring grants and revocation without staff membership. The real HTTP and synthetic browser checks pass; full project verification now has 83 passing tests. The student page explicitly marks exam delivery unavailable until native start/resume/submission is connected, and grant-issuing controls are not yet exposed in the staff UI.

The latest local milestone replaces that delivery placeholder with a basic internal attempt screen and authenticated native start/resume, save and submit calls. It includes question navigation, explicit answer saving/clearing, review flags, countdown, lost-response retries and submission confirmation. Grant-derived identity is checked before and after engine calls; browser-supplied learner or paper overrides are rejected. Unsupported media/formulas and advanced delivery controls fail before committing a new attempt. Staff link issuance remains withheld, and this is not the complete exam integration release. See [current student workflow status](examelite-student-attempts.md).

The private central authoring service now supports free packages through the existing native package controller. It preserves unchanged PDF settings and relationships, creates tags under the central owner, validates existing tag scope, and uses the central actor/revision/retry transaction. Paid package creation and conversion of an existing paid package are explicitly withheld until the commercial workflow is integrated. Native checks cover owned creation, updates, retries, foreign relationships, disabled tags, revoked actors, preserved organisation records and no tenant-workspace provisioning. Existing organisation package/image and central question checks also pass. Private central package read/write routes now provide free-package defaults and native snapshots; their choices exclude paid and foreign packages, and tag choices exclude disabled or foreign tags. Native route tests cover those boundaries and controller-backed saves. The authenticated Tech4Learn gateway now connects central free-package drafts, reads, creation, updates and package/tag choices. It whitelists native fields, rejects paid-package writes and owner/path overrides, verifies free-package snapshots and opaque photo identifiers, and derives the actor solely from the current superadmin session. The native adapter still owns relationship validation and persistence. HTTP checks cover these boundaries and invalid provider responses. The shared superadmin classification form now exposes free-package creation and edits, using central relationship choices and separately scoped drafts. Paid-package editing remains explicitly unavailable.

Private central package image routes now upload, replace and remove bounded raster images using the existing package image adapter, retaining old files that organisation copies may reference. They share central actor checks, version checks and transactional retry identity. Preview reads confine filesystem access and recheck the active credential owner, free-package ownership and current photo reference before returning bytes. Native tests cover replacement, repeated requests, foreign and paid records, malformed inputs, revoked actors, failed-save cleanup, mid-read removal and reference-only deletion. The Tech4Learn gateway and shared package image controls now connect these routes. Image writes require current superadmin authority, bounded upload fields and the saved revision; responses are checked as free-package snapshots. Preview reads reuse bounded raster validation and recheck authority before returning no-store image bytes. Synthetic browser checks cover central package creation, image retry, editor locking, draft byte exclusion, current preview URLs and removal. HTTP tests cover foreign-role denial, query/path overrides, malformed images or replies, paid snapshots and mid-call authority revocation.

The private native central adapter now creates exams and updates their settings through the installed ExamElite controller. It uses the existing native defaults, test-type labels, timezone and revision snapshot, with central actor checks and retry identity. Native scope rules derive packaged exam categories/groups from the selected owned packages and validate standalone category choices. Local tests cover central package/group/language selection, fractional pass thresholds, unchanged organisation exams, stale updates, foreign selections, revoked actors and repeat creates. Private central exam reads and choices are available to the gateway. The Tech4Learn central exam gateway/form and central paper-building actions remain pending; this native slice is not a complete superadmin exam workflow.

Private central paper actions now add/remove owned questions, create/update/remove paper sections, assign owned question-section definitions, set subject timers, activate/deactivate exams and control result visibility. They reuse the same native controller and scope validation as organisation actions, with current central actor checks, revision checks and action-specific retry identity. The bounded attached-question reader derives its owner from the credential and rejects query overrides. Native tests cover paper edits, stale requests, foreign question/section/subject selection, timer overflow, desired result visibility, stable retries and unchanged organisation exams. Central translation/document actions remain unavailable here until their adapters are connected. These controls still need the Tech4Learn gateway and superadmin builder.

The authenticated Tech4Learn gateway now exposes central exam choices and the bounded attached-question reader. It checks stored superadmin authority before and after the provider call, rejects ownership/query overrides, verifies ordered IDs and pagination, and projects only question IDs and bounded wording. HTTP checks cover non-superadmin denial, malformed provider pages, Unicode wording limits, cursor misuse and authority revoked during a request. Central exam settings/action writes and the superadmin builder remain pending; these reads alone do not expose a complete central exam editor.

The central exam gateway and shared superadmin builder now create exams, patch settings and connect the nine native paper actions above. All writes derive the actor from the current superadmin session, whitelist fields/actions, retain revision/request identity and recheck stored authority after the provider call. Gateway metadata checks project bounded native test-type labels, timezone, publication status, sections and subject timings; organisation IDs, timestamps and other extra metadata are not returned. Native relationship and timing validation remains in ExamElite. Central documents and translation editing remain explicitly unavailable in this builder.

The shared builder routes all central choices, saved reads and paper edits to the central adapter, uses separate central settings/section/timer draft keys, and loads the central question bank. Failed reloads hide the stale editor and retain the newly saved exam ID for retry. Synthetic browser verification covers create, publication response loss and identical retry, question-bank selection/addition, failed reload recovery and absence of unconnected document/translation requests. HTTP tests cover current superadmin checks, native error mapping, action/ownership/query rejection and malformed snapshots; a local native-controller paper test also produces snapshots accepted by the gateway metadata checks. These are local checks, not a live deployment confirmation.
